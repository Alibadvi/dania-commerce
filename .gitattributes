* text=auto

# Bash scripts must remain executable inside Linux containers, even when the
# repository is cloned on Windows with core.autocrlf enabled.
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
