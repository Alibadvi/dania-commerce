# Storefront
VENDURE_SHOP_API_URL=http://localhost:3000/shop-api
NEXT_PUBLIC_SITE_URL=http://localhost:3001
ALLOW_DUMMY_PAYMENTS=true

# Docker Compose local defaults (change these if the machine is shared)
DB_NAME=danya
DB_USERNAME=danya
DB_PASSWORD=danya-local-password
DB_SYNCHRONIZE=true
COOKIE_SECRET=danya-local-cookie-secret-change-before-production
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=danya-local-admin-password
